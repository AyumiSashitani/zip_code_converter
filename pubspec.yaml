name: zip_code_converter
description: A new Flutter project.
publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

  # logger
  logger: ^1.1.0

  # http wrapper
  http: ^0.13.4

  # model
  build_runner: ^2.1.10
  freezed: ^2.0.2
  freezed_annotation: ^2.0.1
  json_serializable: ^6.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
flutter:
  uses-material-design: true
